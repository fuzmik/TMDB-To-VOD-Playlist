# .env.example file for TMDB-To-VOD-Playlist Docker environment

# --- PHP Application Settings ---

# TMDB API Key (required)
TMDB_API_KEY=YOUR_TMDB_API_KEY

# Real-Debrid API Token (optional)
REAL_DEBRID_TOKEN=YOUR_REAL_DEBRID_TOKEN

# Premiumize API Token (optional)
PREMIUMIZE_TOKEN=YOUR_PREMIUMIZE_TOKEN

# Host IP address for the PHP application to use for external access and inter-container communication.
# For Linux Docker, this is often the IP of the docker0 interface (e.g., 172.17.0.1).
# Use 'localhost' if running directly on the host and it works.
APP_HOST_IP=172.17.0.1

# Proxy server for the PHP application (e.g., http://user:pass@host:port). Leave empty if not used.
APP_PROXY=

# Set to 'true' to include adult content in VOD playlists.
INCLUDE_ADULT_VOD=false

# Set to 'true' to enable automatic playlist creation (e.g., via cron job).
CREATE_PLAYLIST=false

# --- HeadlessVidX (Node.js) Settings ---

# Port for HeadlessVidX service. Should match the port exposed in docker-compose.yml.
PORT=3202

# Set to 'true' to show the browser window during HeadlessVidX execution (useful for debugging).
SHOW_BROWSER=false

# Proxy server for HeadlessVidX (e.g., http://user:pass@host:port). Leave empty if not used.
HEADLESS_PROXY=

# Set to 'true' to enable the Playwright stealth plugin for better anti-detection.
USE_STEALTH_PLUGIN=true

# Optional: Specify a custom path to the Firefox executable if needed.
# FIREFOX_EXECUTABLE_PATH=/usr/bin/firefox

# Optional: Specify a custom temporary directory for video processing within the container.
# VIDEO_TMP_DIR=/app/videos_tmp
